loader.define(function(requires, exports, module) {
    console.log("photo was loaded")
    var pageview = {
        init: function() {
            // 这里写业务
        }
    };

    // 初始化
    pageview.init();
})