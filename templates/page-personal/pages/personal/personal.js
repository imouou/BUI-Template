/**
 * 个人中心模板
 * 默认模块名: pages/personal/personal
 * @return {[object]}  [ 返回一个对象 ]
 */
loader.define(function(requires, exports, module) {

    var pageview = {
        init: function() {
            // 这里初始化
        }
    };

    // 初始化
    pageview.init();

    // 输出模块
    return pageview;
})