/**
 * 个人中心模板
 * 默认模块名: pages/main/personal
 * @return {[object]}  [ 返回一个对象 ]
 */
loader.define(function(require, exports, module) {

    var pageview = {
        init: function() {
            // 这里写业务
        }
    };

    // 初始化
    pageview.init();
})